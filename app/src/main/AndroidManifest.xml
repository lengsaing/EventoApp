<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.sangleng.evento">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".AuthenticateActivity"
            android:theme="@style/NonActionBarTheme"></activity>
        <activity
            android:name=".PostActivity"
            android:label="@string/title_activity_post"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".DescriptionActivity"
            android:label="DescriptionActivity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".InvoiceActivity"
            android:label="InvoiceActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".SearchQueryActivity"
            android:label="SearchQueryActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>